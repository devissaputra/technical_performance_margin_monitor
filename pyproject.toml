[project]
name = "technical-performance-margin-monitor"
version = "1.2.0"
description = "Fixed-checkpoint NASA battery performance-margin and forecast-maturity research package."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
